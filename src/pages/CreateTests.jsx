import React from "react";

const CreateTests = () => {
    return (
        <div>
            <h1>Upload the PDF and Click on submit button</h1>
        </div>
    );
};

export default CreateTests;
